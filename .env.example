# Get these from the Stripe Dashboard (test mode / sandbox): https://dashboard.stripe.com/test/apikeys
STRIPE_SECRET_KEY=sk_test_...
STRIPE_PUBLISHABLE_KEY=pk_test_...

# Optional: only needed if you're forwarding webhooks locally with the Stripe CLI
# (`stripe listen --forward-to localhost:4242/webhook` prints this value)
STRIPE_WEBHOOK_SECRET=whsec_...

PORT=4242
